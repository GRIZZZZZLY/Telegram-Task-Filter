# Plan: MVP Backend + API — Этап 1

**Дата:** 2026-02-25  
**Проект:** Telegram Task Filter  
**Цель:** минимально рабочий backend с rule engine, SQLite, API эндпоинтами и тестами

---

## Скоуп

### В скоупе
- Pydantic settings (читает `.env`)
- SQLite + SQLAlchemy (create_all, без Alembic)
- ORM-модели: Task, Event
- Pydantic v2 response-схемы
- `GET /health`, `GET /tasks`, `POST /tasks/{id}/done`
- YAML rule engine (mention / keywords / chat_id / thread_id)
- Telegram stub-сервис (mock + TODO)
- Unit-тесты: rule engine, task endpoints
- Обновлённый README + dev-up.sh

### Вне скоупа
- Telethon listener (этап 2)
- WebSocket `/ws/tasks` (этап 2)
- Alembic миграции (этап 2)
- React UI (этап 3)
- AI-классификация (после MVP)

---

## Файлы

### Создать (12 файлов)
```
services/api/app/config.py
services/api/app/database.py
services/api/app/models.py
services/api/app/schemas.py
services/api/app/services/rule_engine.py
services/api/app/services/task_service.py
services/api/app/services/telegram_service.py
services/api/app/routers/tasks.py
services/api/tests/__init__.py
services/api/tests/conftest.py
services/api/tests/test_rule_engine.py
services/api/tests/test_tasks.py
services/api/data/.gitkeep
```

### Изменить (4 файла)
```
services/api/app/main.py
services/api/requirements.txt
services/api/README.md
scripts/dev-up.sh
```

---

## Исправления относительно черновика (все включены)

| # | Проблема | Решение |
|---|----------|---------|
| 1 | `env_file=".env"` резолвится от CWD — не найдёт `.env` из корня | Якорить через `Path(__file__).parents[4] / ".env"` |
| 2 | `rules_path` дефолт — относительный от CWD | То же, якорить через `Path(__file__)` |
| 3 | Тесты создают `test_focus.db` файл | Использовать `sqlite:///:memory:` |
| 4 | `datetime.utcnow()` deprecated в Python 3.12+ | Заменить на `datetime.now(timezone.utc)` |
| 5 | `dependency_overrides` на уровне модуля | Вынести в `conftest.py` + фикстура с `autouse` |
| 6 | `data/*.db` попадёт в коммит | `.gitkeep` + запись в `.gitignore` |

---

## Реализация по файлам

---

### `services/api/requirements.txt`

```
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
telethon>=1.36.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
PyYAML>=6.0
pytest>=8.0
httpx>=0.27
```

---

### `services/api/app/config.py`

```python
"""Application settings via pydantic-settings.
Reads from .env in the project root (auto-detected from this file's location).
"""
from functools import lru_cache
from pathlib import Path

from pydantic_settings import BaseSettings, SettingsConfigDict

# Anchor: services/api/app/config.py → parents[3] = project root
_PROJECT_ROOT = Path(__file__).parents[3]


class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_file=str(_PROJECT_ROOT / ".env"),
        env_file_encoding="utf-8",
        extra="ignore",
    )

    # App
    app_env: str = "dev"
    app_host: str = "127.0.0.1"
    app_port: int = 8787
    app_log_level: str = "INFO"
    app_secret_key: str = "change_me"

    # DB
    db_url: str = f"sqlite:///{_PROJECT_ROOT / 'services' / 'api' / 'data' / 'focus_filter.db'}"

    # Rules — absolute path anchored to project root
    rules_path: str = str(_PROJECT_ROOT / "shared" / "rules" / "default_rules.yaml")

    # UI / CORS
    ui_origin: str = "http://localhost:1420"

    # Telegram
    tg_api_id: str = ""
    tg_api_hash: str = ""
    tg_phone: str = ""
    tg_session_name: str = "user"
    tg_mention_handle: str = "@LindsyQQ"
    tg_monitored_chat_ids: str = ""
    tg_monitored_thread_ids: str = ""

    # Reactions
    done_reaction: str = "✅"


@lru_cache
def get_settings() -> Settings:
    """Singleton per process — safe for FastAPI DI."""
    return Settings()
```

> Почему `parents[3]`: `config.py` → `app/` → `api/` → `services/` → `project_root`.

---

### `services/api/app/database.py`

```python
"""SQLAlchemy engine + session factory + table bootstrap."""
from pathlib import Path

from sqlalchemy import create_engine
from sqlalchemy.orm import Session, sessionmaker

from .config import get_settings


def _make_engine():
    settings = get_settings()
    # Ensure data directory exists (for SQLite file)
    db_path = settings.db_url.replace("sqlite:///", "")
    Path(db_path).parent.mkdir(parents=True, exist_ok=True)
    return create_engine(
        settings.db_url,
        connect_args={"check_same_thread": False},
        echo=(settings.app_env == "dev"),
    )


engine = _make_engine()
SessionLocal = sessionmaker(bind=engine, autoflush=False, autocommit=False)


def get_db():
    """FastAPI dependency — yields a DB session."""
    db: Session = SessionLocal()
    try:
        yield db
    finally:
        db.close()


def create_tables() -> None:
    """Bootstrap schema. MVP: create_all is fine (add Alembic in stage 2)."""
    from .models import Base  # lazy import — avoid circular
    Base.metadata.create_all(bind=engine)
```

---

### `services/api/app/models.py`

```python
"""SQLAlchemy ORM models: Task, Event."""
import enum
from datetime import datetime, timezone
from sqlalchemy import Column, DateTime, Enum, ForeignKey, Integer, String, Text
from sqlalchemy.orm import DeclarativeBase, relationship


def _now() -> datetime:
    return datetime.now(timezone.utc)


class Base(DeclarativeBase):
    pass


class TaskStatus(str, enum.Enum):
    inbox = "inbox"
    done = "done"
    snoozed = "snoozed"


class TaskPriority(str, enum.Enum):
    low = "low"
    medium = "medium"
    high = "high"


class EventType(str, enum.Enum):
    created = "created"
    updated = "updated"
    done = "done"
    reaction_sent = "reaction_sent"
    error = "error"


class Task(Base):
    """A task extracted from a Telegram message."""

    __tablename__ = "tasks"

    id = Column(Integer, primary_key=True, index=True)
    title = Column(String(500), nullable=False)
    body = Column(Text, nullable=True)
    status = Column(Enum(TaskStatus), default=TaskStatus.inbox, nullable=False)
    priority = Column(Enum(TaskPriority), default=TaskPriority.medium, nullable=False)
    chat_id = Column(String(100), nullable=True, index=True)
    thread_id = Column(String(100), nullable=True, index=True)
    source_message_id = Column(Integer, nullable=True)
    created_at = Column(DateTime(timezone=True), default=_now, nullable=False)
    updated_at = Column(DateTime(timezone=True), default=_now, onupdate=_now, nullable=False)

    events = relationship("Event", back_populates="task", cascade="all, delete-orphan")


class Event(Base):
    """Audit event for a task lifecycle action."""

    __tablename__ = "events"

    id = Column(Integer, primary_key=True, index=True)
    task_id = Column(Integer, ForeignKey("tasks.id", ondelete="CASCADE"), nullable=False)
    type = Column(Enum(EventType), nullable=False)
    payload_json = Column(Text, nullable=True)
    ts = Column(DateTime(timezone=True), default=_now, nullable=False)

    task = relationship("Task", back_populates="events")
```

---

### `services/api/app/schemas.py`

```python
"""Pydantic v2 schemas for API request / response."""
from datetime import datetime
from typing import List, Optional

from pydantic import BaseModel, ConfigDict

from .models import EventType, TaskPriority, TaskStatus


class TaskOut(BaseModel):
    """Public representation of a Task."""

    model_config = ConfigDict(from_attributes=True)

    id: int
    title: str
    body: Optional[str] = None
    status: TaskStatus
    priority: TaskPriority
    chat_id: Optional[str] = None
    thread_id: Optional[str] = None
    source_message_id: Optional[int] = None
    created_at: datetime
    updated_at: datetime


class TaskListOut(BaseModel):
    items: List[TaskOut]
    total: int


class EventOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: int
    task_id: int
    type: EventType
    payload_json: Optional[str] = None
    ts: datetime
```

---

### `services/api/app/services/rule_engine.py`

```python
"""Rule engine: evaluates messages against YAML rules.

Rule format (shared/rules/default_rules.yaml):
  - id: my_rule
    name: Human label
    enabled: true
    priority: 100           # higher = evaluated first
    when:
      mention: "@handle"    # optional — exact handle match (case-insensitive)
      keywords: ["todo"]    # optional list — ANY keyword match
      chat_id: "-100123"    # optional — exact string match
      thread_id: "173"      # optional — exact string match
    then:
      create_task: true
      priority: high        # low | medium | high

Interface: intentionally simple so Telethon listener plugs in with no changes.
"""
from dataclasses import dataclass, field
from pathlib import Path
from typing import List, Optional

import yaml

from ..models import TaskPriority


@dataclass
class MessageMeta:
    """Metadata attached to an incoming Telegram message."""

    chat_id: str
    thread_id: Optional[str] = None
    mentions: List[str] = field(default_factory=list)
    sender: Optional[str] = None


@dataclass
class RuleResult:
    """Outcome of evaluating a message against all rules."""

    create_task: bool
    priority: TaskPriority = TaskPriority.medium
    matched_rule_id: Optional[str] = None


class RuleEngine:
    """Loads YAML rules and evaluates messages against them."""

    def __init__(self, rules_path: str) -> None:
        self._rules: list = self._load(rules_path)

    # ── Public API ─────────────────────────────────────────────────────────

    def evaluate(self, text: str, meta: MessageMeta) -> RuleResult:
        """Return RuleResult for the given message text + metadata.

        Rules sorted by 'priority' descending; first match wins.
        """
        active = sorted(
            [r for r in self._rules if r.get("enabled", True)],
            key=lambda r: r.get("priority", 0),
            reverse=True,
        )
        for rule in active:
            if self._matches(rule, text, meta):
                return self._build_result(rule)

        return RuleResult(create_task=False)

    # ── Private helpers ────────────────────────────────────────────────────

    @staticmethod
    def _load(path: str) -> list:
        p = Path(path)
        if not p.exists():
            return []
        with open(p, "r", encoding="utf-8") as fh:
            return yaml.safe_load(fh) or []

    @staticmethod
    def _build_result(rule: dict) -> RuleResult:
        then = rule.get("then", {})
        prio_raw = then.get("priority", "medium")
        try:
            priority = TaskPriority(prio_raw)
        except ValueError:
            priority = TaskPriority.medium
        return RuleResult(
            create_task=bool(then.get("create_task", False)),
            priority=priority,
            matched_rule_id=rule.get("id"),
        )

    @staticmethod
    def _matches(rule: dict, text: str, meta: MessageMeta) -> bool:
        """Return True if ALL 'when' conditions are satisfied."""
        when = rule.get("when", {})

        if "mention" in when:
            handle = when["mention"].lower()
            if not any(m.lower() == handle for m in meta.mentions):
                return False

        if "keywords" in when:
            text_lower = text.lower()
            if not any(k.lower() in text_lower for k in when["keywords"]):
                return False

        if "chat_id" in when:
            if str(meta.chat_id) != str(when["chat_id"]):
                return False

        if "thread_id" in when:
            if str(meta.thread_id or "") != str(when["thread_id"]):
                return False

        return True
```

---

### `services/api/app/services/task_service.py`

```python
"""Task CRUD + done flow."""
import json
from datetime import datetime, timezone
from typing import List, Optional, Tuple

from fastapi import HTTPException
from sqlalchemy.orm import Session

from ..models import Event, EventType, Task, TaskPriority, TaskStatus


def _now() -> datetime:
    return datetime.now(timezone.utc)


class TaskService:
    def __init__(self, db: Session) -> None:
        self.db = db

    def get_tasks(
        self,
        status: Optional[TaskStatus] = None,
        priority: Optional[TaskPriority] = None,
        thread_id: Optional[str] = None,
        limit: int = 50,
        offset: int = 0,
    ) -> Tuple[List[Task], int]:
        """Return (items, total) with optional filters."""
        q = self.db.query(Task)
        if status is not None:
            q = q.filter(Task.status == status)
        if priority is not None:
            q = q.filter(Task.priority == priority)
        if thread_id is not None:
            q = q.filter(Task.thread_id == thread_id)

        total = q.count()
        items = q.order_by(Task.created_at.desc()).offset(offset).limit(limit).all()
        return items, total

    def mark_done(self, task_id: int) -> Task:
        """Set status=done, write 'done' event. Returns updated task.

        Raises:
            HTTPException 404: task not found.
            HTTPException 409: task already done.
        """
        task: Optional[Task] = self.db.get(Task, task_id)
        if task is None:
            raise HTTPException(status_code=404, detail=f"Task {task_id} not found")
        if task.status == TaskStatus.done:
            raise HTTPException(status_code=409, detail=f"Task {task_id} is already done")

        task.status = TaskStatus.done
        task.updated_at = _now()

        event = Event(
            task_id=task.id,
            type=EventType.done,
            payload_json=json.dumps({"task_id": task_id, "ts": _now().isoformat()}),
            ts=_now(),
        )
        self.db.add(event)
        self.db.commit()
        self.db.refresh(task)

        # TODO (stage 2): call TelegramService.send_reaction(task.chat_id, task.source_message_id)

        return task
```

---

### `services/api/app/services/telegram_service.py`

```python
"""Telegram interaction service.

MVP STATUS: STUB / MOCK — не подключается к реальному Telegram.

TODO (stage 2):
  1. Инициализировать Telethon TelegramClient (credentials из Settings).
  2. Реализовать send_reaction() через client.send_reaction().
  3. Подключить start/stop в lifespan FastAPI-приложения.
"""
import logging

logger = logging.getLogger(__name__)


class TelegramService:
    """Interface for Telegram operations triggered by the API."""

    async def send_reaction(
        self,
        chat_id: str,
        message_id: int,
        reaction: str = "✅",
    ) -> bool:
        """Put a reaction on the source Telegram message.

        MVP: logs the call and returns True without touching Telegram.
        """
        logger.info(
            "[MOCK] send_reaction | chat_id=%s message_id=%s reaction=%s",
            chat_id,
            message_id,
            reaction,
        )
        return True
```

---

### `services/api/app/routers/tasks.py`

```python
"""Tasks router: GET /tasks, POST /tasks/{id}/done."""
from typing import Optional

from fastapi import APIRouter, Depends, Query
from sqlalchemy.orm import Session

from ..database import get_db
from ..models import TaskPriority, TaskStatus
from ..schemas import TaskListOut, TaskOut
from ..services.task_service import TaskService

router = APIRouter(prefix="/tasks", tags=["tasks"])


@router.get("", response_model=TaskListOut, summary="List tasks")
def list_tasks(
    status: Optional[TaskStatus] = Query(None, description="Filter by status"),
    priority: Optional[TaskPriority] = Query(None, description="Filter by priority"),
    thread_id: Optional[str] = Query(None, description="Filter by Telegram thread/topic ID"),
    limit: int = Query(50, ge=1, le=200, description="Page size"),
    offset: int = Query(0, ge=0, description="Page offset"),
    db: Session = Depends(get_db),
) -> TaskListOut:
    """Return paginated tasks with optional filters."""
    svc = TaskService(db)
    items, total = svc.get_tasks(status, priority, thread_id, limit, offset)
    return TaskListOut(items=items, total=total)


@router.post("/{task_id}/done", response_model=TaskOut, summary="Mark task done")
def mark_task_done(task_id: int, db: Session = Depends(get_db)) -> TaskOut:
    """Mark task as done. Writes a 'done' event. Returns updated task.

    - **404** if task not found
    - **409** if task already done
    """
    svc = TaskService(db)
    return svc.mark_done(task_id)
```

---

### `services/api/app/main.py` (изменить)

```python
"""FastAPI application entry point."""
import logging
from contextlib import asynccontextmanager

from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

from .config import get_settings
from .database import create_tables
from .routers import tasks

logger = logging.getLogger(__name__)
settings = get_settings()


@asynccontextmanager
async def lifespan(app: FastAPI):
    """Startup: create DB tables. Shutdown: placeholder for Telethon stop."""
    logger.info("Starting up | env=%s db=%s", settings.app_env, settings.db_url)
    create_tables()
    yield
    logger.info("Shutting down")


app = FastAPI(
    title="TG Focus Filter API",
    version="0.1.0",
    lifespan=lifespan,
)

app.add_middleware(
    CORSMiddleware,
    allow_origins=[settings.ui_origin],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(tasks.router)


@app.get("/health", tags=["meta"])
def health():
    """Health check — returns ok + current env."""
    return {"ok": True, "env": settings.app_env}
```

---

### `services/api/tests/conftest.py`

```python
"""Shared test fixtures for the API service."""
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

from app.database import get_db
from app.main import app
from app.models import Base

# In-memory SQLite — fast, no file artifacts
_TEST_DB_URL = "sqlite:///:memory:"


@pytest.fixture(scope="session")
def test_engine():
    engine = create_engine(_TEST_DB_URL, connect_args={"check_same_thread": False})
    Base.metadata.create_all(bind=engine)
    yield engine
    engine.dispose()


@pytest.fixture
def db_session(test_engine):
    """Fresh session per test; rolls back after each test."""
    connection = test_engine.connect()
    transaction = connection.begin()
    Session = sessionmaker(bind=connection)
    session = Session()
    yield session
    session.close()
    transaction.rollback()
    connection.close()


@pytest.fixture
def client(db_session):
    """TestClient with DB dependency overridden."""
    def override_get_db():
        yield db_session

    app.dependency_overrides[get_db] = override_get_db
    with TestClient(app) as c:
        yield c
    app.dependency_overrides.clear()
```

---

### `services/api/tests/test_rule_engine.py`

```python
"""Unit tests for RuleEngine."""
from pathlib import Path

import pytest

from app.services.rule_engine import MessageMeta, RuleEngine

RULES_PATH = Path(__file__).parents[3] / "shared" / "rules" / "default_rules.yaml"


@pytest.fixture
def engine():
    return RuleEngine(str(RULES_PATH))


def test_mention_creates_high_priority_task(engine):
    meta = MessageMeta(chat_id="-100123", mentions=["@LindsyQQ"])
    result = engine.evaluate("Hey @LindsyQQ please review this", meta)
    assert result.create_task is True
    assert result.priority.value == "high"
    assert result.matched_rule_id == "mention_me"


def test_no_mention_no_task(engine):
    meta = MessageMeta(chat_id="-100123", mentions=[])
    result = engine.evaluate("Random message with no trigger", meta)
    assert result.create_task is False


def test_mention_case_insensitive(engine):
    meta = MessageMeta(chat_id="-100123", mentions=["@lindsyqq"])
    result = engine.evaluate("Hey @lindsyqq", meta)
    assert result.create_task is True


def test_empty_rules_file_returns_no_task(tmp_path):
    empty = tmp_path / "empty.yaml"
    empty.write_text("", encoding="utf-8")
    eng = RuleEngine(str(empty))
    meta = MessageMeta(chat_id="-100", mentions=["@LindsyQQ"])
    assert eng.evaluate("anything", meta).create_task is False


def test_missing_rules_file_returns_no_task(tmp_path):
    eng = RuleEngine(str(tmp_path / "nonexistent.yaml"))
    meta = MessageMeta(chat_id="-100", mentions=["@LindsyQQ"])
    assert eng.evaluate("anything", meta).create_task is False


def test_keyword_rule(tmp_path):
    """Custom rule with keyword trigger."""
    yaml_content = """
- id: keyword_rule
  name: Keyword test
  enabled: true
  priority: 50
  when:
    keywords: ["todo", "fix"]
  then:
    create_task: true
    priority: medium
"""
    rules_file = tmp_path / "rules.yaml"
    rules_file.write_text(yaml_content, encoding="utf-8")
    eng = RuleEngine(str(rules_file))
    meta = MessageMeta(chat_id="-100")

    assert eng.evaluate("Please TODO this thing", meta).create_task is True
    assert eng.evaluate("No trigger here", meta).create_task is False
```

---

### `services/api/tests/test_tasks.py`

```python
"""Integration tests for /tasks endpoints."""
import pytest
from sqlalchemy.orm import Session

from app.models import Task, TaskPriority, TaskStatus


# ── helpers ────────────────────────────────────────────────────────────────

def seed_task(db: Session, title: str = "Test task", status: TaskStatus = TaskStatus.inbox) -> Task:
    t = Task(title=title, status=status, priority=TaskPriority.medium)
    db.add(t)
    db.commit()
    db.refresh(t)
    return t


# ── /health ────────────────────────────────────────────────────────────────

def test_health(client):
    r = client.get("/health")
    assert r.status_code == 200
    assert r.json()["ok"] is True


# ── GET /tasks ──────────────────────────────────────────────────────────────

def test_list_tasks_empty(client):
    r = client.get("/tasks")
    assert r.status_code == 200
    body = r.json()
    assert body["total"] == 0
    assert body["items"] == []


def test_list_tasks_returns_seeded(client, db_session):
    seed_task(db_session, "Write tests")
    seed_task(db_session, "Fix bug")
    r = client.get("/tasks")
    body = r.json()
    assert body["total"] == 2
    assert len(body["items"]) == 2


def test_list_tasks_filter_by_status(client, db_session):
    seed_task(db_session, "done task", status=TaskStatus.done)
    seed_task(db_session, "inbox task", status=TaskStatus.inbox)
    r = client.get("/tasks?status=inbox")
    body = r.json()
    assert body["total"] == 1
    assert body["items"][0]["status"] == "inbox"


def test_list_tasks_filter_by_priority(client, db_session):
    t = Task(title="High prio", status=TaskStatus.inbox, priority=TaskPriority.high)
    db_session.add(t)
    db_session.commit()
    seed_task(db_session, "Low prio")  # medium by default

    r = client.get("/tasks?priority=high")
    assert r.json()["total"] == 1


def test_list_tasks_pagination(client, db_session):
    for i in range(5):
        seed_task(db_session, f"Task {i}")
    r = client.get("/tasks?limit=2&offset=0")
    assert len(r.json()["items"]) == 2
    assert r.json()["total"] == 5


# ── POST /tasks/{id}/done ───────────────────────────────────────────────────

def test_mark_done_success(client, db_session):
    task = seed_task(db_session)
    r = client.post(f"/tasks/{task.id}/done")
    assert r.status_code == 200
    body = r.json()
    assert body["status"] == "done"
    assert body["id"] == task.id


def test_mark_done_creates_event(client, db_session):
    from app.models import Event, EventType
    task = seed_task(db_session)
    client.post(f"/tasks/{task.id}/done")

    event = db_session.query(Event).filter(
        Event.task_id == task.id,
        Event.type == EventType.done,
    ).first()
    assert event is not None


def test_mark_done_404(client):
    r = client.post("/tasks/99999/done")
    assert r.status_code == 404


def test_mark_done_409_already_done(client, db_session):
    task = seed_task(db_session, status=TaskStatus.done)
    r = client.post(f"/tasks/{task.id}/done")
    assert r.status_code == 409
```

---

### `services/api/README.md` (перезаписать)

```markdown
# API Service — TG Focus Filter

FastAPI backend: хранит задачи, оценивает правила, управляет событиями.

## Быстрый старт

```bash
# из корня репозитория
cp .env.example .env          # заполни значения при необходимости

cd services/api
python3 -m venv .venv
source .venv/bin/activate     # Windows: .venv\Scripts\activate
pip install -r requirements.txt

uvicorn app.main:app --reload --host 127.0.0.1 --port 8787
```

Swagger UI: http://127.0.0.1:8787/docs

## curl-примеры

```bash
# Health check
curl http://localhost:8787/health

# Список всех задач (inbox)
curl "http://localhost:8787/tasks?status=inbox&limit=20"

# Список задач с фильтром по приоритету
curl "http://localhost:8787/tasks?priority=high&status=inbox"

# Пагинация
curl "http://localhost:8787/tasks?limit=10&offset=20"

# Закрыть задачу
curl -X POST http://localhost:8787/tasks/1/done
```

## Тесты

```bash
cd services/api
pytest tests/ -v
```

## Структура

```
app/
  config.py           — pydantic-settings (читает .env из корня проекта)
  database.py         — SQLAlchemy engine + get_db dependency
  models.py           — Task, Event ORM-модели
  schemas.py          — Pydantic v2 response-схемы
  main.py             — FastAPI app, CORS, lifespan
  routers/
    tasks.py          — GET /tasks, POST /tasks/{id}/done
  services/
    rule_engine.py    — YAML rule evaluator
    task_service.py   — CRUD + done логика
    telegram_service.py — STUB реакции (TODO: Telethon, этап 2)
  workers/            — зарезервировано под Telethon listener (этап 2)
tests/
  conftest.py         — общие фикстуры (in-memory SQLite)
  test_rule_engine.py — unit-тесты rule engine
  test_tasks.py       — интеграционные тесты endpoints
data/
  .gitkeep            — placeholder; focus_filter.db создаётся при старте
```

## Переменные окружения (ключевые)

| Переменная | Дефолт | Описание |
|------------|--------|----------|
| `DB_URL` | `sqlite:///...data/focus_filter.db` | Путь к SQLite |
| `RULES_PATH` | `shared/rules/default_rules.yaml` | Правила извлечения задач |
| `UI_ORIGIN` | `http://localhost:1420` | CORS origin для Tauri UI |
| `DONE_REACTION` | `✅` | Реакция при закрытии задачи |
| `APP_ENV` | `dev` | dev / prod |
```

---

### `scripts/dev-up.sh` (изменить)

```bash
#!/usr/bin/env bash
set -e

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

echo "=== [1/2] Starting API ==="
cd "$ROOT/services/api"

if [ ! -d ".venv" ]; then
  echo "  → Creating venv..."
  python3 -m venv .venv
fi

# shellcheck source=/dev/null
source .venv/bin/activate
pip install -r requirements.txt -q

uvicorn app.main:app --reload --host 127.0.0.1 --port 8787 &
API_PID=$!
echo "  → API pid=$API_PID  http://127.0.0.1:8787/docs"
echo "  → Swagger: http://127.0.0.1:8787/docs"

echo ""
echo "=== [2/2] Desktop UI ==="
echo "  → cd apps/desktop && npm install && npm run tauri dev"
echo ""
echo "Press Ctrl+C to stop API."
wait $API_PID
```

---

### `services/api/data/.gitkeep`

_(пустой файл — создаёт директорию в git)_

---

### Запись в `.gitignore` (добавить)

```gitignore
# SQLite databases
services/api/data/*.db
services/api/tests/*.db
*.db
```

---

## Acceptance Criteria

| # | Критерий | Проверка |
|---|----------|----------|
| AC-1 | `GET /health` → `{"ok": true, "env": "dev"}` | `curl /health` |
| AC-2 | `GET /tasks` с пустой БД → `{"items": [], "total": 0}` | test_list_tasks_empty |
| AC-3 | `POST /tasks/{id}/done` меняет статус → done | test_mark_done_success |
| AC-4 | Повторный done → 409 | test_mark_done_409 |
| AC-5 | Несуществующий id → 404 | test_mark_done_404 |
| AC-6 | После done создаётся Event type=done | test_mark_done_creates_event |
| AC-7 | Mention @LindsyQQ → create_task=True, priority=high | test_mention_creates_high_priority_task |
| AC-8 | Нет mention → create_task=False | test_no_mention_no_task |
| AC-9 | Все тесты зелёные: `pytest tests/ -v` | CI / local |

---

## Этап 2 — следующий шаг

1. **Telethon listener** (`workers/tg_listener.py`): слушает чаты, парсит упоминания, вызывает `RuleEngine.evaluate()`, создаёт `Task`
2. **Реальные реакции**: подключить `TelegramService` к Telethon client (убрать mock)
3. **WebSocket** `WS /ws/tasks`: push новых задач в UI
4. **Alembic миграции**: заменить `create_all`
5. **React UI**: список задач + кнопка Done

---

## Риски

| Риск | Митигация |
|------|-----------|
| `.env` не найден при нестандартном CWD | Путь якорится через `Path(__file__).parents[3]` — не зависит от CWD |
| `rules_path` не найден | `RuleEngine._load` возвращает `[]`, не падает |
| `create_all` без миграций | Приемлемо для MVP single-user SQLite |
| `TelegramService` — только stub | `[MOCK]` в логе + явный TODO |
